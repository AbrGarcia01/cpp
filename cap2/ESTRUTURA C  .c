// ESTRUTURAS C++

[<declarações de pré-processamento - cabeçalhos>]
[<declaração do nome de ambiente para definição de seu escopo>]
[<declaração das variáveis globais>]
int main([<parâmetros])
{
    /*
    Este trecho é reservado para o corpo da função, com a
    declaração de suas variáveis locais, seus comandos e funções de trabalho
    */
    retuner 0;
    }

    [
        [<tipos>] Funçãonome([<parâmetros>])
        [<declaração de parâmetros>]
        {
        /*
        Este trecho é reservado para o corpo da função, com a
        declaração de suas variáveis locais, seus comandos e funções de trabalho
        */
        [return; ou return(); ou return(valor);]
        }
    ]
}
